name: Daily Changes Summary

on:
  schedule:
    - cron: '30 15 * * 1-5'   
  workflow_dispatch:

jobs:
  summary:
    uses: org-test-reply-1/platform-ai-workflows/.github/workflows/ai-daily-summary.yml@a87037605c174207eab693e2a868d5b7712a63bb
    secrets:
      COPILOT_ASSIGN_PAT: ${{ secrets.COPILOT_ASSIGN_PAT }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
